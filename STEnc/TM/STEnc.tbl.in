# Cambridge Bay Sun Tracker Enclosure Display
STEnc {
  HBox { +-; "Enclosure"; -+ };
  Position:  (STEnc_pos,7);
  Command:   (STEnc_cmd,7);
}

STEnc_HK {
  HBox { +-; "Enclosure HK"; -+ };
  Bits:      (STEnc_bits,4);
  Cmds:      (STEnc_cmds,2);
  PDISO8:    (STEnc_fail,4);
  Stale:     (STEnc_stale,3);
}
